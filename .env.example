# Postgres connection string used by Prisma
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/poshpilot"

# NextAuth configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-32-byte-secret"

# OAuth (optional but recommended)
GITHUB_ID=""
GITHUB_SECRET=""
NEXT_PUBLIC_GITHUB_ENABLED="false"

# Base64 encoded 32 byte symmetric key for libsodium secretbox
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=""
